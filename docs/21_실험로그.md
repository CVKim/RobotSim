# 실험 로그

> 규칙: run마다 한 줄 — 무엇을/왜/결과. 상세 수치는 explore/·runs/의 JSON이 원본.

## 2026-09-01

| # | 실험 | 결과 | 비고 |
|---|---|---|---|
| 1 | .mim 포맷 해독 | Matrox MIL(TIFF), ToF 640×480 float32 X/Y/D/I(mm), RGB 2448×2048 | tifffile로 로드 |
| 2 | 빈피킹 상면 검출 v1 | 30세션 148박스, L=293.0±9.2 / W=218.8±9.9mm, 층고 283mm | 깊이 모폴로지 실패 → **I채널 에지 분리가 해법** |
| 3 | 픽포인트+슬롯 추적 v2 | 기울기 평균 0.95°(max 2.7°), 평면 RMS 5.4mm, 픽 순서 0→9 자동 복원 | RMS가 노이즈 모델과 정합 |
| 4 | ToF 시간 노이즈 실측 | **σ(mm)=180.3×I^-0.805** (정적 픽셀 12.7만) | 거리 의존은 비단조 → 강도 지배. DR 캘리브레이션 근거 |
| 5 | 대차 후크 검출 | 4/4 성공, 폭 42~48mm, 평면高 반복성 1.2mm std | 카메라 피치 50°·어안 휨 보정 필요했음 |
| 6 | 팔레타이징 env v1 (cap30) | DBL 52.8% — **보상 상수 결함 발견** | 전 에피소드 30박스 도달 → 학습신호 없음 |
| 7 | 팔레타이징 env v2 (cap120) | **DBL 베이스라인: 56.6±4.1박스, 활용률 59.8%, 5층** | PPO 목표선 |
| 8 | MaskablePPO 학습 (GPU1) | **완료: 64.9박스/활용률 68.5%±0.6 — DBL(56.6/59.8%) 대비 +14.7%/+8.7%p** (1.5M 스텝, 동일시드 100ep 평가) | runs/palletize_ppo, 곡선: 36→64 수렴 (500k 이후 플래토) |
| 9 | PushT Diffusion (GPU0) | 1차: `--eval_freq` 인자 오류 / 2차: accelerate 누락 → `[training]` extra 설치 후 3차 기동 | runs/pusht_diffusion |

## 환경 이슈 기록

- **torch 2.9~2.11 전 빌드가 Windows 26200에서 c10.dll WinError 1114** (CPU 빌드 포함, 페이지파일·AVX·런타임 전부 무관) → **2.8.0+cu126로 해결** (pytorch#166628 참고, 2.9+ 리그레션)
- lerobot 0.6.1: 학습엔 `lerobot[training]` extra 필수(accelerate), env는 `[pusht]` 등 별도
- torchcodec DLL 로드 실패는 pyav 폴백으로 무해 (Windows+torch<2.8 계열 알려진 제약)
- cv2.imread 한글 경로 실패 → `imread_kr()`(np.fromfile+imdecode)
- PS 콘솔 cp949: 파이썬 print에 ±·≈·한글 시 UnicodeEncodeError 주의

## 2026-09-02 (심야 자동 진행)

| # | 실험/사건 | 결과 | 비고 |
|---|---|---|---|
| 10 | 인식-정책 통합 데모 | 실제 ToF 상태 -> PPO가 빈 슬롯을 NEXT로 정확 제안 | tools/perception_to_policy.py |
| 11 | PushT 20k에서 크래시 | 원인: 체크포인트 `last` 심볼릭 링크가 WinError 1314(개발자 모드 필요) | lerobot train_utils에 copytree 폴백 패치 -> 20k에서 resume |
| 12 | SmolVLA 기동 실패 | 원인: HF repo id가 Windows 경로 정규화로 `\` 변환 | 모델 로컬 다운로드(models/smolvla_base) 후 절대경로로 재기동 |
| 13 | 감시 필터 교훈 | HFValidationError/OSError를 좁은 필터가 놓침 | 통합 모니터로 교체(광범위 Error 매칭, torchcodec 제외) |
| 14 | PICK NEXT (최소 동선 픽 선택) | 실제 픽 순서 검증: 거리순위 평균 1.69, 그리디 일치 50%/top-2 81% | 사용자 제안 반영, tools/pick_next.py |
| 15 | 이적재 공정 복원 | 목적지 스택 성장 770->1260mm 계단 확인, 반출 시 0 복귀 | transfer_conservation.png |
| 16 | SmolVLA-450M 파인튜닝 (GPU1) | **완료**: loss 0.636→0.128 수렴, 20k steps = 2h06m, **VRAM 피크 4718MiB/10GB** (BS2, vision 동결 100M 학습) | svla_so101_pickplace 50ep. 성능 평가는 시뮬/실기 필요 — 자원 실측이 목적. BS4~6 여유 확인 |
| 17 | BlenderProc SDG 1차 (T5) | 물리드롭+탑다운 렌더 성공, 합성 상면깊이 2982 vs 실측 2973mm (9mm 정합) | 시행착오: import 첫줄 규칙, 렌더패스 1회 규칙, 카메라 -Z 기본. 이슈: 인스턴스 라벨 병합(수정 예정) |
| 18 | sim2real 갭 v1 (300씬) | clean: 실측 mAP50 0.000 / noisy: 0.106 — 노이즈 주입 효과 확인, 지배 요인은 배경 사실성 | v2: 바닥깊이 랜덤(3.3~4.3m)+방해물 8개+대면적 블롭 무효+정규화 2600~4300 |
| 19 | 픽 순서 규칙 마이닝 | 불일치 전수 = 같은 열에서 더 먼 박스 우선 패턴 -> 열 스캔 규칙로 예측 정확도 50->81% | 실제 설비가 열 단위 far->near 스캔으로 추정. 잔여 3건은 열 내부 순서 |
| 20 | sim2real 갭 v4 | **noisy 실측 mAP50 0.432** (v1 0.106 대비 4배) / clean 0.000 — 무효픽셀·노이즈 시뮬이 결정 요인임을 ablation으로 입증 | 배경깊이 랜덤+방해물+블롭 무효. 다음: 실측 프레임 혼합, 세그 모델 |

## 2026-09-03

| # | 실험 | 결과 | 비고 |
|---|---|---|---|
| 21 | PushT Diffusion 스윕 (50ep x 6ckpt) | 성공률 38%(20k) -> 24%(60k) -> 38%(100k), 최대 커버리지 85~90% | 비단조 곡선 확보. forkserver->sync env 수정 필요했음 |
| 22 | 모방학습 풀사이클 (gym-hil Franka) | 완벽 데모 BC=0% -> **DART(노이즈 주입 복구 데이터) BC=100%** (20 데모만으로) | covariate shift 진단->해결. 전문가 SR 100%, 그리퍼 방향/mocap 게인 디버깅 포함 |
| 23 | 인프라 이슈 | run_in_background 래퍼에서만 mujoco 에셋 로드 실패 (원인 미상) -> 스테이지 분할 포그라운드로 우회 | 재현 조건 기록 |
| 24 | sim2real v5 co-training | **합성+실측 6장 -> 홀드아웃 24장 mAP50 0.988** (합성 전용 0.428) | 사다리 완성: 0.00(clean)->0.43(노이즈시뮬)->0.99(+6실측). 주의: pseudo-GT 동질성·장면 균질성 언급 필요 |
| 25 | 후크 반복성 v2 (ICP) | 4세션 raw RMS 22.7mm -> ICP 정렬 17.6mm -> 바이어스 세션(141923, 병합-분리 경로) 제외 시 **8.2mm** | 실이동/검출바이어스/센서를 분리. 검출 개선이 다음 지렛대 |
| 26 | Isaac Sim 4.5 설치 (pip, py310) | 헤드리스 스모크 부팅 성공 (첫 구동 31분 — 확장 컴파일) | .venv_isaac. 다음: Replicator로 박스 씬 재현(BlenderProc 결과와 비교) |
| 27 | VLM 에이전트 데모 (로컬 Qwen2.5-VL-3B) | 3세션: 박스 수 그라운딩 100%(11/7/3 정확), 도구 슬롯 채택 100%, **pick 결정 100%**, 유효 JSON 100%, VRAM 7.3GB | v1 프롬프트에선 전부 "wait"로 회피 → "스캔 완료·도구 권위" 규칙 명시 후 해결. 데이터 외부 유출 없음 |
| 28 | SDG 툴 비교 (Isaac Replicator vs BlenderProc) | 동일 노이즈 레시피·실측 홀드아웃 24장: **Isaac 0.555 vs BlenderProc 0.428** mAP50 | Isaac: 정치 배치+RTX 조명 / BP: 물리드롭+Cycles. 레시피 차이 교락 있음. Isaac 300프레임 생성 수 분 |
| 29 | 실용성 검증: 지연·강건성 | **55ms/프레임(CPU 전단계)**. 열화 리콜: 스펙클 30%→100%(치수 -3% 바이어스), 깊이노이즈 20mm→91%, 강도대비 0.1x→100%, 대면적 결손 15/30개→86%/72% | 최대 약점 = 대면적 결손(가림). 1 SKU·1카메라 한계 명시 |

## 2026-09-04

| # | 실험 | 결과 | 비고 |
|---|---|---|---|
| 30 | 후크 검출 v2 (강도 분할 벽면 + 정렬 방식 4종) | 4세션 반복성 RMS-3D: raw 22.7 → 전체장면 ICP 17.6 → **데크(고정 구조물) ICP 2.9(v1 중심)/3.2mm(v2 벽면)**, 면내 1.84mm | 이전 25mm '바이어스'는 대차 실이동 21~52mm 를 전체장면 ICP 가 흡수 못한 정렬 오류. 추정기보다 정렬 기준이 지배. tools/hook_analysis_v2.py |
| 31 | 학습 세그 (YOLO11n-seg, BP 276 + Isaac 276 noisy + 실측 6) | holdout 24 box/mask mAP50 0.978(v1-GT); 열화 리콜: gauss 20mm 0.99(기하 0.90), 블롭 30개 0.76(기하 0.66) — **스펙클 ≥15% 에서 0.00 붕괴**(기하 0.99); 미디언5 전처리로 15% 0.96 / 30% 0.40 복구 | GPU 21.9 / CPU 44ms. 학습 분포 밖 무효 픽셀 밀도 취약 → 스펙클 증강 필요. tools/seg_dataset.py, seg_train_eval.py |
| 32 | robotsim_perception 패키지 | 순수 numpy/opencv 패키지 + CLI + JSON 스키마 1.0 + 합성 프레임 생성기; 파리티 148/148; **pytest 27 passed**; 인식 연산 51.8ms | docs/40. 알려진 한계: 경계 침식 -3~-7%, 최상층 피크 가정 |
| 33 | 기하 검출 v2 (격자 결손 보완 + 층 피크 재시도) + 정답 재검증 | 30프레임 RGB 대조(로컬): **실박스 167, v1 148(88.6%)** — 2층 4×3 중앙 2개 병합 실패(7프레임 13박스) + 층 말미 잔여 1~2개 피크 전환 누락(4프레임 6박스, 상위 8피크 재시도로 회수). **v2 167/167, 빈 프레임 오검출 0, 226ms**. 열화(3시드 평균) 블롭 30개 위치일치 리콜 v1 56.5% → v2 74.2%, gauss 20mm 80% → 99%, 스펙클 30% 89% → 99% | 처음엔 v2 추가 검출을 오검출로 오판 — 셀 내 상면 픽셀 88~100%·모순 픽셀 0 을 확인하고 RGB 대조로 실박스 판정. 슬롯 추적 '정답'은 v1 순환 참조였음. tools/robustness_bench_v2.py |
| 34 | 학습 모델 v2-GT 재평가 | holdout 24 GT 115 → 128박스. cotrain det mAP50 0.988 → 0.983 (**P 0.949 → 0.998**, R 1.00 → 0.945), seg 0.978 → 0.991 (P 0.956 → 1.000), BP-noisy 0.430 → 0.481, Isaac-noisy 0.556 → 0.565 | 기존 '오검출'은 v1 이 놓친 실박스 → 정밀도 과소평가였음. 리콜 0.945 = 병합 중앙 박스 일부 미검출. tools/reeval_v2_gt.py, explore/real_eval_v2 |

## 2026-09-04 (Week 1 — 셀 디지털 트윈)

| # | 실험 | 결과 | 비고 |
|---|---|---|---|
| 35 | MuJoCo 셀 트윈 구축 | 실측 역산 지오메트리(카메라 4.183m·f517·데크 0.643m·박스 293×219×283·피치 300×225)로 MJCF 생성. **렌더 상면 2970mm vs 실측 2973mm 일치**. 강도는 I·D²≈3527 실측 관계로 모델링 | sim/cell_scene.py, sim/cell_twin.py. 트윈 1차 시도에서 평평한 바닥이 깊이 히스토그램을 지배해 검출 실패 → 주변 설비를 높이 다양하게 채워 실측 장면 구성 재현 |
| 36 | 검출기 결함 2건 수정 (트윈이 발견) | (a) **센티넬 오염**: MORPH_CLOSE 가 메운 무효 픽셀의 X/Y 센티넬이 minAreaRect 에 섞여 13m 사각형 → 종횡비 필터 탈락. **실측에서도 4박스 누락 중이었음 (v1 148→152)**. (b) **층 선택**: 질량 1위 피크가 바닥일 때 재고 불가 → 'strong≥2 중 최근접' 규칙으로 교체 | tools/binpick_topface.py + robotsim_perception/detect.py 동시 수정, 패키지 파리티 152=152 유지. 실측 v2 167 및 세션별 카운트 불변. v2 지연 226→341ms |
| 37 | 절대 GT 검출 평가 (48장면) | **중심오차 클린 7.8mm [CI 6.2,9.7] / 노이즈 10.2mm [8.1,12.6]**, p95 25.9/42.9mm. 치수 바이어스 L −8.1/−15.4mm (경계 침식을 절대값으로 확인). 각도 4.8/5.4°, 깊이 +0.0mm. 리콜 잔여≥4개 0.89/0.82 | tools/twin_detect_eval.py. **미해결 실패 모드**: 가득 찬 아래층 위 잔여 ≤3개면 층 선택기가 아래층으로 점프 → 정밀도 0.12~0.14 (실측 시퀀스엔 없는 배치) |

## 2026-09-04 (Week 2 — 로봇 좌표 배관 + 폐루프)

| # | 실험 | 결과 | 비고 |
|---|---|---|---|
| 38 | 로봇 좌표 변환·6-DoF 포즈 모듈 | `T_base_cam` 4x4 + `PickPose`(위치·접근벡터·요·pre/post-pick) + `suction_footprint_ok`(컵 아래 유효픽셀·평탄도). 그동안 출력이 전부 카메라 좌표 mm 라 로봇이 실행 불가였던 배관을 채움 | robotsim_perception/pose.py. 실제 T_base_cam 값은 핸드아이 캘리브레이션 필요(하드웨어) — 구조·왕복검증만 제공 |
| 39 | 인식→제어 폐루프 (셀 트윈) | 10에피소드x6박스=60회. **oracle 96.7% vs 인식구동 56.7% → 인식 비용 40.0%p**. 인식 실패 내역: 소스 미검출 6, 풋프린트 거부 5, 흡착실패 2 | tools/twin_closed_loop.py. 정책은 인식 출력만 사용(정답 위치 미사용). 지배 실패 = 잔여 소수 층 선택 오류가 폐루프 손실로 전이 |
| 40 | 폐루프 하네스가 드러낸 설계 누락 3건 | (a) 소스/목적지 ROI 구분 없어 방금 옮긴 박스를 재픽(같은 박스 4회) (b) 목적지 배치 계획 없어 한 점에 낙하·붕괴 → 4x3 격자 슬롯 (c) 흡착 판정이 '최근접 박스'라 옆 박스를 어긋나게 물음 → 측면 90mm/수직 45mm 분리 | 전부 실제 셀에 필요한 요소. MuJoCo weld 런타임 활성화 시 기본 relpose 가 항등이라 박스가 0.8m 튕김 → 운반은 운동학적 부착으로 모델링 |

## 2026-09-04 (트윈 충실도 교정 + 층 선택 완화)

| # | 실험 | 결과 | 비고 |
|---|---|---|---|
| 41 | 트윈 실패 모드가 진짜인지 실측으로 검증 | **실측 고립 박스(잔여 1~3개, n=12)는 L 293.7±7.1 / W 216.2±7.1 로 정상 측정** (조밀 프레임 292.6/218.2와 동일). 트윈의 W 219→132mm(−40%) 침식은 **트윈 아티팩트**. 세션 380406 은 트윈 실패 구성(가득 찬 1층 위 2층 3개)과 동일한데 실측은 3개를 정확히 찾는다 | 검출기를 트윈 신호에 맞춰 튜닝했다면 존재하지 않는 문제를 고칠 뻔했다. 트윈 수치 검증의 필요성 사례 |
| 42 | 트윈 충실도 교정 (설비 높이) | 설비 반높이 0.12~0.62m → 상단 1.24m → **깊이 2.94m 로 박스 상면층(2.97m)과 겹침**. 실측 셀엔 그 높이 설비가 없다. 0.10~0.44 로 제한 → 트윈 정밀도 0.62→0.79, 잔여≥4 구간 **0.72→0.95** | 트윈의 낮은 정밀도 주원인이 검출기가 아니라 내 씬 구성이었음 |
| 43 | 층 선택 임계 완화 | 'strong≥2 중 최근접' → **'strong≥1 중 최근접'**. 설비 상한 수정으로 바닥층 허위 strong 이 사라져 안전해짐. 트윈 잔여 2개 케이스 해결(FP 6→0), recall 0.71→0.77 / precision 0.56→0.66. **실측 167 및 세션별 카운트 불변** | 잔여 1·3개는 미해결. 폐루프에서는 임계 1/2 결과 동일(단층 시나리오라 경쟁 층 없음) |
| 44 | 폐루프 재측정 | oracle **100%** (60/60), 인식 구동 46.7% (28/60) → 인식 비용 53.3%p. 실패: 소스 미검출 6, 흡착 실패 5, 전 후보 거부 3. 풋프린트 거부 시 다음 후보를 시도하도록 수정(사이클 낭비 제거) | 이전 56.7% 와는 씬 구성이 달라 직접 비교 아님. 이 시나리오는 후반부가 전부 잔여≤3 구간이라 미해결 실패 모드 영향이 최대 |

## 2026-09-04 (검출 알고리즘 단일 원본화 + v2 패키지 이식)

| # | 실험 | 결과 | 비고 |
|---|---|---|---|
| 45 | 중복 구현 제거 | 검출 알고리즘이 tools/binpick_topface.py 와 robotsim_perception/detect.py 에 **각각 복제**돼 있어 센티넬 오염 버그를 두 번 고쳐야 했다. `robotsim_perception/geometry.py` 로 단일화하고 tools 는 재수출로 전환 (12개 스크립트 import 그대로 유지) | v1 152 / v2 167, 세션별 카운트 불변 |
| 46 | v2 패키지 이식 | 패키지에 `detect_boxes(frame, lattice=True)` 및 CLI `--lattice` 노출. 박스에 `source`(detected/inferred) 추가. 배포 모듈이 **152 → 167박스** | 지연 52 → 약 340 ms 이므로 기본값은 v1 유지 |
| 47 | 전 세션 파리티 테스트 | 기존 파리티 테스트는 **세션 1개만** 검사해 복제 시절의 불일치(148 vs 152)를 놓쳤다. 30세션 전체 × (v1, v2) 비교 테스트 추가 → **28 passed** | test_parity_all_sessions_v1_and_v2 |
