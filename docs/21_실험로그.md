# 실험 로그

> 규칙: run마다 한 줄 — 무엇을/왜/결과. 상세 수치는 explore/·runs/의 JSON이 원본.

## 2026-09-01

| # | 실험 | 결과 | 비고 |
|---|---|---|---|
| 1 | .mim 포맷 해독 | Matrox MIL(TIFF), ToF 640×480 float32 X/Y/D/I(mm), RGB 2448×2048 | tifffile로 로드 |
| 2 | 빈피킹 상면 검출 v1 | 30세션 148박스, L=293.0±9.2 / W=218.8±9.9mm, 층고 283mm | 깊이 모폴로지 실패 → **I채널 에지 분리가 해법** |
| 3 | 픽포인트+슬롯 추적 v2 | 기울기 평균 0.95°(max 2.7°), 평면 RMS 5.4mm, 픽 순서 0→9 자동 복원 | RMS가 노이즈 모델과 정합 |
| 4 | ToF 시간 노이즈 실측 | **σ(mm)=180.3×I^-0.805** (정적 픽셀 12.7만) | 거리 의존은 비단조 → 강도 지배. DR 캘리브레이션 근거 |
| 5 | 대차 후크 검출 | 4/4 성공, 폭 42~48mm, 평면高 반복성 1.2mm std | 카메라 피치 50°·어안 휨 보정 필요했음 |
| 6 | 팔레타이징 env v1 (cap30) | DBL 52.8% — **보상 상수 결함 발견** | 전 에피소드 30박스 도달 → 학습신호 없음 |
| 7 | 팔레타이징 env v2 (cap120) | **DBL 베이스라인: 56.6±4.1박스, 활용률 59.8%, 5층** | PPO 목표선 |
| 8 | MaskablePPO 학습 (GPU1) | **완료: 64.9박스/활용률 68.5%±0.6 — DBL(56.6/59.8%) 대비 +14.7%/+8.7%p** (1.5M 스텝, 동일시드 100ep 평가) | runs/palletize_ppo, 곡선: 36→64 수렴 (500k 이후 플래토) |
| 9 | PushT Diffusion (GPU0) | 1차: `--eval_freq` 인자 오류 / 2차: accelerate 누락 → `[training]` extra 설치 후 3차 기동 | runs/pusht_diffusion |

## 환경 이슈 기록

- **torch 2.9~2.11 전 빌드가 Windows 26200에서 c10.dll WinError 1114** (CPU 빌드 포함, 페이지파일·AVX·런타임 전부 무관) → **2.8.0+cu126로 해결** (pytorch#166628 참고, 2.9+ 리그레션)
- lerobot 0.6.1: 학습엔 `lerobot[training]` extra 필수(accelerate), env는 `[pusht]` 등 별도
- torchcodec DLL 로드 실패는 pyav 폴백으로 무해 (Windows+torch<2.8 계열 알려진 제약)
- cv2.imread 한글 경로 실패 → `imread_kr()`(np.fromfile+imdecode)
- PS 콘솔 cp949: 파이썬 print에 ±·≈·한글 시 UnicodeEncodeError 주의

## 2026-09-02 (심야 자동 진행)

| # | 실험/사건 | 결과 | 비고 |
|---|---|---|---|
| 10 | 인식-정책 통합 데모 | 실제 ToF 상태 -> PPO가 빈 슬롯을 NEXT로 정확 제안 | tools/perception_to_policy.py |
| 11 | PushT 20k에서 크래시 | 원인: 체크포인트 `last` 심볼릭 링크가 WinError 1314(개발자 모드 필요) | lerobot train_utils에 copytree 폴백 패치 -> 20k에서 resume |
| 12 | SmolVLA 기동 실패 | 원인: HF repo id가 Windows 경로 정규화로 `\` 변환 | 모델 로컬 다운로드(models/smolvla_base) 후 절대경로로 재기동 |
| 13 | 감시 필터 교훈 | HFValidationError/OSError를 좁은 필터가 놓침 | 통합 모니터로 교체(광범위 Error 매칭, torchcodec 제외) |
| 14 | PICK NEXT (최소 동선 픽 선택) | 실제 픽 순서 검증: 거리순위 평균 1.69, 그리디 일치 50%/top-2 81% | 사용자 제안 반영, tools/pick_next.py |
| 15 | 이적재 공정 복원 | 목적지 스택 성장 770->1260mm 계단 확인, 반출 시 0 복귀 | transfer_conservation.png |
| 16 | SmolVLA-450M 파인튜닝 (GPU1) | **완료**: loss 0.636→0.128 수렴, 20k steps = 2h06m, **VRAM 피크 4718MiB/10GB** (BS2, vision 동결 100M 학습) | svla_so101_pickplace 50ep. 성능 평가는 시뮬/실기 필요 — 자원 실측이 목적. BS4~6 여유 확인 |
